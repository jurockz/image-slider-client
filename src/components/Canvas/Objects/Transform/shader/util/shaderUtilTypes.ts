import { vertexR } from "../../Vertex/types"

interface calculateCornerRadiusPositionProps {
  vertexA: vertexR,
  vertexB: vertexR,
  cornerRadius: number
}

export {
  calculateCornerRadiusPositionProps
}